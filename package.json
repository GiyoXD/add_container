{
  "name": "vision-logistics-app",
  "version": "1.0.0",
  "scripts": {
    "build": "node build.js",
    "cap-copy": "npx cap copy",
    "cap-sync": "npx cap sync",
    "android-open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.4",
    "@capacitor/cli": "^8.3.4",
    "@capacitor/core": "^8.3.4"
  }
}
